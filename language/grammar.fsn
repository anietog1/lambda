# TERMINALS

ID := '[a-z]'
LAMBDA := '\'
DOT := '.'
LPAREN := '('
RPAREN := ')'

# NON-TERMINALS

expression ::= '\' ID '.' expression
             | application

application ::= application atomic
              | atomic

atomic ::= ID
         | '(' expression ')'
