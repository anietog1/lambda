# TERMINALS

ID := '[_a-zA-Z][_a-zA-Z0-9]*'

# NON-TERMINALS

abstraction ::= '\' ID '.' abstraction
             | application

application ::= application atomic
              | atomic

atomic ::= ID
         | '(' abstraction ')'
